#include "holberton.h"

/**
* print_diagonal - draws a diagonal line in the terminal.
* @n length of line
* Return: nothing
**/

void print_diagonal(int n)
{
	if (n > 0)
	{
		int count;

		count = 0;

		while (count < n)
		{
			_putchar('\\');
			count++;
		}
	}
		_putchar('\n');
}
