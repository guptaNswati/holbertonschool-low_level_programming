#include <stdlib.h>
#include <stdio.h>
#include "holberton.h"

/**
* computes the absolute value of an integer.
* Returns: an int
**/

int _abs(int n)
{
	int result;
	result = abs(n);
	return result;
}
