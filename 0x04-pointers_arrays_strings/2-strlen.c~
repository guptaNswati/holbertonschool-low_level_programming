#include <string.h>
#include "holberton.h"

/**
* _strlen - returns the length of a string.
* @*s: a pointer to a char
* Return: length of a string
**/

int _strlen(char *s)
{
	int length;
	char str[20];
	strcpy(str, *s);

	length = strlen(str);
	return (length);
}

