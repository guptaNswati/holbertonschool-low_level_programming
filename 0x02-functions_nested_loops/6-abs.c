#include <stdlib.h>
#include "holberton.h"

/**
* _abs - computes the absolute value of an integer.
*
* @n: int to be evaluated
*
* Return: absolute value of an int
**/

int _abs(int n)
{
	int result;

	result = abs(n);
	return (result);
}
